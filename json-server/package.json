{
  "name": "json-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn run json-server --watch db.json --host 'https://restaurant-website-app.herokuapp.com' --port $PORT --static ../build",
    "dev": "yarn run json-server --watch db.json -d 2000 -p 3001"
  },
  "dependencies": {
    "express": "^4.17.1",
    "json-server": "^0.16.1"
  }
}
